name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: |
          cd ~/APLICATIE
          git checkout -- .
          git pull origin main

      - name: Build backend
        run: |
          cd ~/APLICATIE
          ./mvnw clean package -DskipTests

      - name: Restart backend
        run: |
          pm2 restart star-arena-backend

      - name: Build frontend
        run: |
          cd ~/APLICATIE/frontend-dragos
          npm ci
          npm run build

      - name: Deploy frontend
        run: |
          sudo cp -r ~/APLICATIE/frontend-dragos/dist/* /var/www/html/
          sudo nginx -s reload
